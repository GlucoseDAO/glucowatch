[project]
name = "glucowatch-scripts"
version = "0.1.0"
description = "Developer tools for screenshots, the icon, store images, and the Nightscout replay server. Not part of the app."
requires-python = ">=3.11"
dependencies = [
    "pillow>=12.3.0",
]

[tool.uv]
package = false
