<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Heart rate for the footer's [HEART_RATE]. The watch asks the user when the face is
         chosen; without it the face shows a dash. Wear OS 6 replaced BODY_SENSORS with
         the per-type health permission. -->
    <uses-permission
        android:name="android.permission.BODY_SENSORS"
        android:maxSdkVersion="35" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />

    <!-- Watch Face Format: declarative only, no code. -->
    <application
        android:hasCode="false"
        android:label="@string/watchface_name">
        <property
            android:name="com.google.wear.watchface.format.version"
            android:value="1" />
        <property
            android:name="com.google.wear.watchface.format.publisher"
            android:value="glucowatch" />
    </application>
</manifest>
